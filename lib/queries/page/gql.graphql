query Page($id: String!) {
  heritage(id: $id) {
    recipesCollection {
      items {
        title
        uid
        thumbnail {
          title
          url
        }
      }
    }
    seo {
      ...Seo
    }
  }
}
