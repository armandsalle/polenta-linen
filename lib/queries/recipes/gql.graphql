query AllRecipes {
  recipeCollection {
    items {
      uid
      tags
      title
      thumbnail {
        title
        url(transform: { quality: 80, width: 600 })
      }
      sys {
        id
      }
      subtitle
    }
  }
}
